# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'OpenMovieApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for OpenMovieApp

  pod 'SwiftyJSON', '~> 4.0'

  target 'OpenMovieAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'OpenMovieAppUITests' do
    # Pods for testing
  end

end
